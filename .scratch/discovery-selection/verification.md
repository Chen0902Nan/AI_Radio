# 探索选歌验收记录

日期：2026-09-18。默认 50% 歌单内 / 50% 探索。

## 流程与范围

用户已确认规格与三个公开测试入口：选歌 HTTP、播放记录 HTTP、浏览器播放操作。使用 Matt Pocock 的小步 TDD 与最终双轴审查；保留此前栈迁移工作，不提交或推送。由于已有 apps/packages 未跟踪迁移，本次审查以本功能文件和规格为范围，不把整个 HEAD diff 当成本次改动。

## 红灯到绿灯

- 重复播放实例最初插入两条记录；添加实例唯一键后同一次通知只记一次。
- 混合选歌最初仅返回红心、没有来源；接入完整曲库/推荐候选与服务端归属后约各半返回。
- 初次开播缺少混合准备入口；接入 startListening 后准备期间保持静音，实际出声带来源及实例标识。
- 手动来源入口缺失；接入 selectManual 后手动歌曲不计数、结束接回自动电台。
- 停止早于 session/start 返回时留下活动会话；增加迟到收尾后通过。
- 审查发现探索超时占满共用期限，公开 HTTP 回归复现失败；改为分来源计时后仍能用歌单内歌曲兜底。

## 本地验证

- 最终全量 `npm test`：163/163 通过（包括审查修复）；`npm run typecheck` 通过。
- 新增 `scripts/tests/discovery-http.test.cjs`：真实 Nest HTTP + SQLite；音乐平台接口与 Codex 输出采用隔离替身。13/13 通过，覆盖滚动窗口、进程重建、版本反馈、部分资料、试听、来源失败、超时预算。
- `apps/web/test/radio-runtime.test.cjs`：24/24 通过，覆盖首批、记录身份、手动接续及迟到请求；媒体/API 为可控替身。
- 新增真实 Chrome 用例已通过：运行生产构建、实际 WAV 解码，验证首批等待、停止不误播、来源记录与手动歌单接续。供应商/HTTP 为 fixture，不代表真实供应商完整端到端。
- 最终浏览器全套：`node --test --test-force-exit --test-timeout=90000 scripts/browser/*.test.mjs`，3/3 通过。原有播放/DJ 回归改为显式选择红心歌单，普通混合首批由新用例覆盖。
- 第一次批量浏览器执行因测试进程未退出而中止；单独核验原回归并用有界测试命令重跑三项均通过。该退出问题不作为页面功能通过证据。
- 原始输出：[本地测试](test-results.log)、[浏览器测试](browser-results.log)。

## 真实网易云只读核验

见 [原始结果](live-readonly.json)：读取完整曲库 709 首，推荐/相似候选 43 首，排除曲库后 39 首；抽查 3 首均取得 full 音源。未写回账号，未使用第三方解锁。

## 审查与修复

规格轴：切换歌单必须立即取消旧准备及防 A/B 请求乱序；探索有候选但无完整音源必须提示退化；首批准备期间禁止下一首播放旧红心队列。均已修复。

代码质量轴：缺失上游数组不得当空集合；音源探测需要次数/时间上限与逐次失效检查；删除旧选歌降级/缓存死路径，测试候选入口跟随新策略。已修复。

两轴共同发现探索与库内兜底不能共用耗尽预算，已回归修复。

## 边界

未执行真实两小时收听，未重启用户已有服务。真实 Codex 与网易云音频完整浏览器串联尚未作为本轮验收；已有订阅接入仍沿用原实现。本轮证据是本地功能测试、隔离浏览器与真实音乐资料/音源只读核验。
