# 第一阶段验证报告：音乐资料 → 完整歌曲 → 本地网页播放

日期：2026-09-14（修订：2026-09-15 补充代码审查修复与回归）
范围：只验证「读取本人网易云音乐资料」「获取有权播放的完整歌曲」「本地网页播放」，含下一首与单曲失败换歌。不含 DJ、TTS、天气、飞书日历、聊天、每日调度。
结论：**链路打通，14 项浏览器检查全部通过**。官方网易云 CLI 路线因缺少开放平台凭据受阻，本轮改用参考方案里的音乐 API 方向。完整产品仍未实施、未验收。
后续代码审查又发现了三个缺陷，已修复并补了能稳定复现的回归验证（见第 7 节）。

---

## 1. 采用的接入方案及版本

| 组件 | 版本 / 位置 | 用途 |
| --- | --- | --- |
| Node.js | v24.16.0 | 运行时 |
| `@neteasecloudmusicapienhanced/api` | 4.40.1（项目内 `node_modules`） | 网易云数据读取与音源查询 |
| `@music163/ncm-cli`（官方） | 0.1.7（项目内安装，未使用） | 仅用于核实接入前提 |
| `puppeteer-core` | 25.11.0（devDependency） | 驱动系统 Chrome 做真实浏览器验证 |
| Google Chrome | 152.0.7977.83（headless=new，系统安装版） | 真实播放验证 |
| 系统 | macOS 26.6.2 (arm64) | — |

**为什么不是官方 CLI。** 官方 `@music163/ncm-cli@0.1.7` 已在本机安装并核查：未配置凭据时任何命令（含 `--help`）都直接退出；用临时 HOME 写入假凭据后，`--help` 与 `commands` 只列出静态命令（play/pause/state/login/tui/configure 等），**`search`、`playlist`、`user favorite` 等资料命令来自远端动态 manifest，必须有有效的开放平台 appId + privateKey 才会注册**。凭据来自网易云音乐开放平台个人入驻，本机没有。另外该 CLI 的播放由 mpv 承担，不能用来验证网页播放；也没有找到稳定公开、无播放副作用的取音源命令。

**因此采用 API 方向，边界明确：**
- 只用标准只读接口：`/api/song/like/get`、`/api/v3/song/detail`、`/api/user/playlist`、`/api/v6/playlist/detail`、`/api/song/enhance/player/url/v1`、登录相关接口。
- **不传 `unblock` / `match` 参数，不启用任何额外音源**，只返回当前账号本身有权播放的内容。
- 登录用手机扫码（`ncm-cli` 之外的独立流程），cookie 只写本机 `data/session.json`（权限 0600），前端永远拿不到 cookie，也拿不到带鉴权参数的 CDN 直链（音频经本服务转发）。
- 额外副作用说明：首次运行会向网易注册一个**匿名设备 token** 用于辅助查询，这不修改用户的红心、歌单或任何音乐数据。账号内没有任何写操作。

本轮验证使用的账号：`Pluto_Magic`，uid `1734543760`（VIP）。

---

## 2. 音乐资料读取结果

证据文件：`.scratch/radio-agent/verification/artifacts/library-read.json`

| 项目 | 结果 |
| --- | --- |
| 红心歌曲 id | 456 条，去重后 456 条（无重复） |
| 红心歌曲详情 | 456/456 全部取到，缺失 0 |
| 红心总时长 | 约 32.4 小时 |
| 收藏/自建歌单 | 4 个自建 + 2 个收藏 = 6 个，无重复 id |
| 歌单曲目完整性 | 456/456、36/36、60/60、95/95、50/50、57/57，全部 `playlist_detail.trackIds` 一次取全 |

红心样本（前 5 首）：`爱情转移`/陈奕迅/259.99s、`好久不见`/陈奕迅/250.54s、`断桥残雪`/许嵩/227.16s、`梦想天空分外蓝`/陈奕迅/198.61s、`雀跃`/任然·小来/235.65s。

### 分页检查（有实测发现）

- 红心歌曲 `/api/song/like/get` **一次返回全部 id，没有服务端分页**；歌曲详情按 300 条分块请求（456 条 → 2 块）。
- 歌单列表 `/api/user/playlist` 的实际行为和直觉不同，实测确认：
  - **`limit` 参数被服务端忽略**（传 `limit=2` 仍返回全部 6 个）；
  - **`offset` 正常生效**（`offset=6` 起返回 0 条）；
  - **`more` 恒为 `false`**，不能用来判断是否还有下一页。
  - 首版实现依赖 `more` 判断，只是"碰巧正确"。已改成按实际返回条数推进 `offset` + 去重，并用 `pageSize=2/4/50` 三种尺寸复测，结果集完全一致（`pageSize=2/4` 时真的走了 2 页）。
- 错误处理：不存在的歌曲 id（`999999999999`）不抛异常，被正确归类为"不可播放"；接口失败会带 code 抛出，不会当成空歌单。

---

## 3. 网页音源与完整播放

证据文件：`.scratch/radio-agent/verification/artifacts/browser-playback.json`（含 4 张截图与完整网络轨迹）

### 3.1 音源权限分类

对前 12 首红心歌曲逐个查音源：**11 首完整歌曲、1 首无权限、0 首试听片段**。
- 完整歌曲：`br=320000`，文件 7–11 MB，与曲目真实时长一致。
- 无权限样本：`残酷月光`（林宥嘉，id 108557），接口不返回地址，被判为不可播放。
- 试听片段：本账号是 VIP，未出现只有试听权限的曲目，**分类逻辑已实现但未用真实样本验证**。

### 3.2 真实浏览器播放记录

浏览器：系统 Chrome 152，headless=new，通过 puppeteer-core 驱动；播放策略设为 `document-user-activation-required`，即**必须先有点击才允许出声**。

| 观察项 | 记录 |
| --- | --- |
| 页面加载后是否出声 | 否，`paused=true` 且 `<audio>` 无 src |
| 点击播放 | `爱情转移`（id 65536，陈奕迅，曲目时长 260.0s） |
| 播放器报告的时长 | 259.99s，与曲目真实时长一致（证明是完整歌曲，不是试听片段） |
| 自然播放结束 | `ended` 事件 `currentTime=259.986667`、`duration=259.986667` |
| 墙钟耗时 | 260.0s（与真实时长差 < 0.1s，没有加速、没有跳段） |
| 进度采样 | 174 个采样点，进度单调递增，最大单点跨度未超过阈值（未拖动进度） |
| 点击下一首 | 65536 → 65538 `好久不见`，确认为另一首真实可播歌曲 |
| 结束后自动续播 | `ended` 后自动切到 65538，符合预期 |

网络轨迹：音频请求全部走本服务 `/api/audio/<id>`，返回 200/206（206 为 Range 分段），无跨域或鉴权头问题。

### 3.3 故障注入与恢复

| 场景 | 做法 | 结果 |
| --- | --- | --- |
| 单曲音源失败 | `POST /api/_test/fail-next`（仅 `RADIO_TEST_HOOKS=1` 时开放）注入 1 次解析失败 | 该曲（id 167937）被标记失败并暂停旧音频，**1.2s 后自动换到 id 25638273 `梦想天空分外蓝` 并恢复播放**；实测退避 1205ms |
| 连续失败 | 注入 20 次 | 连续 3 首失败后**停止自动换歌**并显示「连续 3 首无法播放，已停止自动换歌」；观察 10.5s 内解析尝试只增加 3 次，**没有快速重试** |
| 真实无权限曲目（非注入） | 直接点击 `残酷月光`（id 108557） | 明确提示「账号当前无播放权限」，自动换到 id 26584453 `梦伴` 并继续播放 |

---

## 4. 交付物与复现方式

```
server/netease.js      接入层：扫码登录、资料读取、音源解析与分类、地址缓存
server/codex.js        Codex 选歌适配层：codex exec 子进程、结构化输出、结果校验、超时处理
server/db.js           本地持久化（Node 内置 node:sqlite）：设置、反馈、会话、播放记录
server/index.js        HTTP 服务：静态页 + JSON API + /api/audio/:id 音频转发（含 Range）+ /api/plan + 会话/反馈 API
public/                最小播放器：开播/继续/停止、下一首、状态提示、Codex 选歌、喜欢/不喜欢
scripts/read-library.mjs     资料读取与分页/去重检查（输出不含凭据的报告，失败时非零退出）
scripts/lib/library-checks.mjs  资料报告的判定逻辑（单独抽出以便测试）
scripts/verify-playback.mjs  真实浏览器播放与故障恢复验证
scripts/verify-fixes.mjs     播放控制/缓存/退出码的回归验证（含敏感性验证）
scripts/verify-codex.mjs     Codex 选歌验证（真实调用、结果校验、失败续播）
scripts/verify-session.mjs   收听会话 / 播放记录 / 喜好反馈验证
data/                  登录会话与 SQLite 数据库（已 gitignore，权限 0600）
```

```bash
npm install
npm start                       # 正常启动，http://127.0.0.1:8787
node scripts/read-library.mjs   # 读取真实资料（需已登录），失败时退出码非零
# 验证用（带故障注入）：
npm run start:test              # RADIO_TEST_HOOKS=1 node server/index.js
npm run verify:playback         # 真实浏览器播放验证（约 5~6 分钟）
npm run verify:fixes            # 播放控制/缓存/退出码回归（约 2 分钟）
npm run verify:codex            # Codex 选歌验证（约 3 分钟，含一次真实 Codex 调用）
npm run verify:session          # 收听会话/反馈验证（约 3 分钟）
```

登录：浏览器打开 `http://127.0.0.1:8787/login`，用手机网易云音乐 App 扫码。二维码只在本机使用。

## 5. 未验证 / 未完成

- **试听片段（trial）分支**：本账号是 VIP，未遇到真实样本。
- **真实 CDN 地址过期**：无法让某个真实播放地址自然失效。现已通过注入“刷新后确认不可播放”，验证了缓存失效与换歌路径（第 7 节），但“地址真的过期时浏览器报错 → 刷新 → 换歌”这条完整链路的中间一环仍未用真实失效地址跑过。
- **超过 6 个歌单、多个分页的真实多页场景**：账号只有 6 个歌单，`limit` 又被服务端忽略，多页收敛主要靠合成数据之外的推演。
- **官方 `ncm-cli` 的音源能力**：缺开放平台凭据，完全未验证。
- 音频输出是 headless Chrome 的真实解码播放，没有用系统扬声器做人工听感确认。
- 未做 2 小时连续收听验收，未实施 DJ/TTS/天气/日历/聊天/调度。

## 6. 下一步建议

1. 若希望回到官方 CLI 路线，需要先完成网易云音乐开放平台个人入驻（appId + privateKey），这属于你本人要做的账号操作；拿到后再复核动态命令的真实输出与音源权限。
2. 播放地址刷新路径仍缺一环真实失效地址的验证，可以用本地可控代理让某个地址失效来补。
3. 进入后续开发前，先把"音源解析 + 待播队列 + 失败策略"从本轮的验证代码里收敛成编排层可复用的接口，避免第二套并行实现。

---

## 7. 修复记录：代码审查发现的缺陷

审查证据：`.scratch/radio-agent/verification/artifacts/fixes-regression.json`（18 项检查全通过）

### 第一轮：A/B/C/D

### 缺陷 A／B：播放控制竞态（`public/app.js`）

- **现象**：快速连续点下一首时，先发出的解析请求可能后返回，把用户最后选中的歌覆盖掉，播放器实际放的是旧的那首；解析过程中点暂停，晚到的解析结果又会把播放重新拉起来。
- **原因**：`playIndex` 只按 `await` 顺序落结果，没有"这次结果还属不属于当前意图"的概念；暂停也没有取消在途请求的途径。
- **修复**：引入播放意图代次号 `playToken`（每次播放/暂停/切歌自增）和 `userWantsPlayback`。异步结果回来时代次不匹配就直接丢弃；暂停会自增代次作废所有在途请求。解析期间按钮显示"暂停"，让用户有取消入口；失败退避定时器也会检查用户是否已经暂停，不再复活播放。

### 缺陷 C：音源缓存不随身份和权限变化失效（`server/netease.js`）

- **现象**：刷新确认某曲已不可播放、或退出登录后，仍可能命中旧缓存里的播放地址。
- **原因**：`resolveTrack` 先查缓存再确认会话，缓存条目也不绑定身份；且只在"有地址"时写缓存，没地址时不清除旧条目。
- **修复**：缓存条目绑定身份（cookie 截断哈希，不保存明文）；查询前先确定身份，身份不匹配直接作废；地址有效则写缓存，无地址/不可播放则删除旧条目；登录与退出登录时整体清空缓存。响应里增加 `identityKind`（user/anon）便于观察。

### 缺陷 D：资料读取脚本会误报成功（`scripts/read-library.mjs`）

- **现象**：资料缺失、分页不一致或部分歌单查询失败时，报告里虽然写了，但脚本仍以 0 退出，只看退出码会把失败当成功。
- **原因**：判定逻辑散在流程里，只有"未登录"一处分了退出码。
- **修复**：判定逻辑抽到 `scripts/lib/library-checks.mjs` 的 `collectFailures()`，统一检查红心去重与详情缺失、分页一致性、歌单完整性与重复、音源查询报错、各步骤成败；失败时写进报告并 `process.exitCode = 1`。

### 第二轮：P1/P2/P3（播放控制的残留问题）

- **P1 暂停后恢复播错歌**：切歌加载中暂停，再点播放会恢复上一首，但界面显示的是新歌。根因是按钮的恢复分支只看 `audio.currentTime > 0`，没有检查播放器里装的是不是列表当前这首。修正：新增 `loadedSrcId()`，只有已加载的音频就是列表当前曲目时才直接续播，否则重新按当前曲目播放。
- **P2 旧定时器切走用户选择**：某首歌失败后会排一个 1.2s 的自动换歌定时器；若用户在这段时间里手动选了别的歌，定时器仍会触发并把它切走。修正：失败时记住当时的 `playToken`，定时器触发时代次变化就说明用户已有新意图，不再执行。
- **P3 暂停后自动出声**：用户暂停之后到达的媒体错误，会在地址刷新成功后自动播放。修正：`refreshCurrent()` 仍会刷新地址，但只在 `userWantsPlayback` 为真时才 `play()`；暂停期间只换地址不出声。错误处理里也区分了"暂停时到达的错误"和"播放中断"。

### 回归验证（`npm run verify:fixes`）

| 检查 | 方法 | 结果 |
| --- | --- | --- |
| A 快速切歌不被旧请求覆盖 | 用请求拦截让先发出的解析请求最晚返回（1600/800/0ms），断言最终 `audioSrc` 与 `currentId` 一致 | 通过 |
| B 加载中暂停不被拉起 | 人为拖慢解析 1.8s，加载中点暂停，等迟到结果返回 | 通过 |
| P1 加载中暂停后恢复播对歌 | 切歌加载中暂停→再点播放，断言播放器实际地址等于界面当前曲目 | 通过 |
| P2 定时器不切走手动选歌 | 注入失败后手动选另一首，越过 1.2s 退避再断言仍是手动选的那首 | 通过 |
| P3 暂停期间媒体错误不出声 | 注入音频流 502 并让元素重发真实请求，断言仍为暂停、但地址已刷新 | 通过 |
| C 刷新确认不可播放后清缓存 | 注入"刷新返回不可播放"，再普通查询，断言 `cached=false` | 通过 |
| D 退出登录后不复用旧身份缓存 | 临时备份会话文件→退出登录→断言 `identityKind=anon` 且 `cached=false`→恢复会话 | 通过 |
| E 判定逻辑与退出码 | 9 组合成报告的判定自检 + 真实让脚本进入失败分支检查退出码 | 通过（退出码 2） |

**测试有效性（敏感性）验证**：把 `public/app.js`、`server/netease.js` 的修复临时回退到修复前的版本，A/B/P1/P2/P3 与 C 均稳定失败（实测两轮都验证过）；恢复修复后全部通过。这排除了"测试永远通过"的可能。

测试设计上踩过并修正的坑：

- 注入点原先自己删缓存，属于自己验证自己。现在注入只替换上游返回值，分类与缓存处理走正常路径。
- 竞态原先靠网络抖动碰巧触发，改为确定的延迟序列。
- 触发"暂停期间的媒体错误"时，先用了 `audio.load()`，但 Chrome 会用媒体缓存直接回应、根本不发网络请求，错误也就不会发生；而且 `preload="none"` 时改了 src 也不会去拉。现改为显式允许预加载并对同一首歌发一次真实请求，并用一条独立断言确认确实出现过 502（否则上一条可能空过）。

### 修复后的完整回归

- `npm run read-library`：退出码 0，`failures: []`、`ok: true`
- `npm run verify:playback`：14 通过 / 0 失败 / 0 跳过
- `npm run verify:fixes`：18 通过 / 0 失败

---

## 8. Codex 选歌最小验证

证据文件：`.scratch/radio-agent/verification/artifacts/codex-plan.json`（13 项检查全通过）

### 接入方式

| 项 | 值 |
| --- | --- |
| CLI | `codex-cli 0.153.4`（/Applications/ChatGPT.app/Contents/Resources/codex） |
| 认证 | 复用本机已登录的 ChatGPT/Codex 订阅，未使用 API key |
| 调用参数 | `codex exec -C <空目录> --skip-git-repo-check --sandbox read-only --ephemeral --ignore-user-config --color never --output-schema <schema> -o <out> <prompt>` |
| 结构化输出 | JSON Schema 约束 `{picks:[{id,reason}]}`，从 `-o` 读最终消息，不解析事件流 |
| 默认超时 | 90s，超时直接 SIGKILL 子进程 |

参数选择上的实测对比：不加 `--ignore-user-config` 时会加载用户的 skills 上下文（约 2.7 万 token 且有截断警告）；加上之后降到约 2.1～2.3 万 token，认证不受影响（认证走 CODEX_HOME，与 config.toml 无关）。**每次调用约 2 万 token**，这是订阅额度上的实际开销，接入编排层时需要控制调用频率。

### 真实调用结果

- 单次调用：exit code 0，耗时 12.4～13.1s，22,711 tokens（含提示词中的 60 首候选）。
- 输入：从 456 首红心歌曲随机抽 60 首作候选，加一句收听场景描述。
- 输出示例（“深夜安静、适合一个人听”）：

| 歌曲 | 选歌理由 | 可播性 |
| --- | --- | --- |
| 喜欢 — 陈奕迅 | 轻柔而亲密，适合让一天的喧闹慢慢退去。 | 完整 |
| 红豆 — 王菲 | 细腻悠缓，让独处时的思念有处安放。 | 完整 |
| 绵绵 — 陈奕迅 | 低回的情绪适合夜深时独自回望往事。 | 完整 |
| 寻人启事 — 徐佳莹 | 留白与倾诉感交织，适合安静听见自己的心事。 | 完整 |
| 儿歌 — 张悬 | 朴素温柔，给深夜的独处一个安心的收尾。 | 完整 |

### 结果校验（模型输出当不可信输入）

- **id 必须在候选集里**：编造的 id 直接丢弃并记录原因。
- **不能重复、必须有理由**：缺理由或重复的条目丢弃。
- **可播性二次校验**：模型给的 id 只是候选，进入队列前逐个用真实账号查音源，不可完整播放的降级丢弃；全部不可播则整体回退。
- **部分非法时保留合法部分**：实测注入“编造 id + 真实 id”混合输出，返回 `ok:true` 且 `kept=2、rejected=2`，被拒项都带原因。
- **全部非法**：判为 `invalid_output`，不进入队列。

### 浏览器验证

- Codex 选歌接入队列时**没有打断正在播放的歌曲**：接入前后 `currentId` 不变、仍处于播放状态。
- 队列结构：原曲留在队首，Codex 选出的 5 首接在其后。
- 界面上逐首显示中文选歌理由，队列里的曲目带 Codex 标记。
- 点 Codex 选出的第一首（`喜欢` — 陈奕迅，id 326719，时长 279.93s）**在网页里真实播放**。

### 失败续播

三种失败都通过测试注入制造，并对**同一个固定基线**断言（避免前一个用例破坏状态后，后面的“未变化”变成空过）：

| 失败类型 | 结果 |
| --- | --- |
| timeout | 队列与选歌结果均未变化、当前曲目未变、仍在播放（进度继续推进 2.81s），界面显示“继续使用原队列，播放不受影响” |
| quota | 同上 |
| invalid_output | 同上，提示具体是“2 条全部不合法” |

### 敏感性验证

把两处临时改坏：(a) 失败分支里清空队列；(b) 去掉“id 必须在候选集里”的校验。结果：校验逻辑自检、部分非法处理、以及三种失败续播共 5 项检查全部稳定失败；恢复后 13 项全通过。

### 未验证 / 限制

- 只验证了“红心歌曲内部选歌”，规格中的 70/30 探索比例、喜好反馈权重、避免短时重复都未实现。
- 未接 DJ 串场、天气、日历、每日调度。
- 真实超时/额度不足未从真实 Codex 侧触发（会消耗订阅额度或需要等真实限流），用的是可控注入；真实的 codex 失败分类（退出码与 stderr 文本）只做了代码实现，未用真实失败样本验证。
- 选歌质量没有量化评估，只有单次样本的目视合理性。
- 每次约 2 万 token，未做长时高频调用的额度影响评估。

（注：反馈权重与避免短时重复已在第 9 节落地并验证。）

---

## 9. 收听会话、播放记录与喜好反馈

证据文件：`.scratch/radio-agent/verification/artifacts/session-feedback.json`（25 项检查全通过）

这一节对应规格「拟采用的默认行为」里的第 1、4、5、10 条，也是 Q23 确认后第一块不依赖任何新凭据的实施。

### 本地存储

用 **Node 24 内置的 `node:sqlite`**，没有引入原生依赖或第三方包。四张表：

| 表 | 用途 | 关键约束 |
| --- | --- | --- |
| `settings` | 可调设置 | 候选数量、选取数量、喜欢/不喜欢权重、避免重复窗口 |
| `feedback` | 显式喜好反馈 | 部分唯一索引保证同一首歌只有一条生效；撤销是标记 `revoked_at`，不删历史 |
| `sessions` | 收听会话 | 含开始/结束时间、结束原因、本次临时调整 |
| `plays` | 播放记录 | 挂在会话上，带 `ended_at` 与 `outcome` |

### 收听会话

- 会话由**服务端**持有，网页只是客户端。点击开播时建立；已存在则复用（`reused: true`）。
- **页面刷新只重连同一会话**，不新建会话、不自动出声，按钮从“开播”变成“继续”。实测刷新前后 `sessionId` 一致，且服务端仍只有一个未结束会话。
- 停止会结束会话并清空本次临时调整；再次开播一定是新会话（实测新 id、`adjustments` 为空）。
- 服务重启时把上一条没结束的会话补写结束时间与原因 `server_restart`，**不删数据**，避免网页刷新“复活”一个早就不在播的会话。

### 喜好反馈

- 喜欢 / 不喜欢 / 撤销三个操作，同一首歌改主意时会先撤销旧反馈再写入新的，同一时刻只有一条生效。
- 撤销是标记而非删除，历史可回溯。
- 非法反馈类型返回 400 且不写库。

### 反馈真的影响选歌

反馈不是只存起来：候选抽样改成**带权不放回抽样**，权重全部来自设置项。

| 场景 | 250 轮 × 每轮 20 首的命中次数 |
| --- | --- |
| 无反馈（基线） | 15 |
| 标记“不喜欢”（权重 0.0001） | 0 |
| 标记“喜欢”（权重 4） | 45 |

两点值得说明：一是“不喜欢”是**降低权重而不是封禁**，把权重压到 0.0001 仍保留被抽中的可能；二是这是用真实抽样函数跑的真实统计，不是断言内部实现。另外“避免短时间重复”也接在同一处：45 分钟内播过的曲在抽样中降权到 0.2。

### 播放记录

开始播放时写一条带会话的 `plays`，结束/跳过/失败/停止时回填 `outcome`。这同时是后续“避免重复”和两小时连续收听证据的基础。

### 这一节的限制

- **明确点歌优先**仍未实现（没有聊天入口）。
- 会话的临时调整只有存取接口，还没有从聊天或界面产生的入口。
- 播放记录的 `outcome` 目前只覆盖到 stopped/skipped/failed/ended 四种，未做统计报表。
- 抽查的反馈权重结论是统计性的，用 250 轮样本控制抖动；换账号或换曲库规模后数值会变，但机制不变。

### 回归

改完这一块后重跑了全部验证：`verify:playback` 14/14、`verify:fixes` 18/18、`verify:codex` 13/13、`read-library` 退出码 0、`verify:session` 25/25。

---

## 10. 自动补歌与统一节目编排

日期：2026-09-16
范围：把「队列、补歌、播放意图、失败恢复」收敛成职责清晰的编排逻辑；待播不足时后台准备下一批；Codex 失败降级与退避；暂停／停止／刷新／切歌的意图边界；候选不足的有界处理。**不含** DJ/TTS、天气、飞书日历、聊天、每日调度与 70%／30% 探索。
结论：**编排链路打通。第一轮 `verify:orchestration` 33 项通过；复核发现两个真实缺陷并修复后，`verify:orchestration` 40 项全通过（含一次真实 Codex 补歌与一次自然播完的跨批续播）**，原有回归全部通过。两小时连续收听与 DJ／播报仍未验收。

### 10.1 关键设计取舍

**队列仍在网页端，服务端只负责「准备下一批」。** 只有网页掌握 `<audio>` 与用户手势，若把队列搬到服务端就要再做一套播放调度；本次因此复用已验证的播放意图代次机制，不新增并行播放器。服务端新增的 `server/orchestrator.js` 只做候选抽样、Codex 调用、可播性校验与补歌协调。

**补歌只追加、不替换。** 网页端 `appendPicks()` 按 id 去重后接到队尾，并排除当前曲、已待播曲与已失败曲；下一首、点歌、点「让 Codex 选歌」等用户操作都不会被后台结果覆盖。

**三层防重复生成。** 客户端同一时刻只允许一个在途补歌（`RefillController.inFlight`）；服务端按 `sessionId + epoch` 复用同一个在途 Promise（同意图并发请求只生成一批）；结果进入队列前再按 id 二次去重。

**意图代次决定结果是否还生效。** 客户端每次取消（停止、切换来源、应用新一批）自增 epoch，迟到结果按 epoch 丢弃；服务端在收到更高 epoch 时把旧任务标记作废，停止会话时调用 `invalidateSession()`。暂停**不作废**后台补歌，但结果只追加、不会自动出声。

**失败恢复分两层。** Codex 超时／额度／输出无效时，服务端降级为「从已取得的曲库候选里挑可播的」，并给该会话记指数冷却（60s→15min 上限），冷却期内不再调用订阅；客户端对「候选耗尽／无可播／音乐不可用」做 30s→5min、最多 5 次的指数退避，超过后进入等待恢复，需要用户动作才再试，不用无限重试掩盖问题。

**队列末尾等下一批。** `next()` 到末尾时，如果补歌在途或还能重试，就进入「等待后台补充」而不是宣告结束；批次到达后接着播。

**为后续统一编排留接口。** 队列条目统一带 `type`（当前只有 `track`）；非 track 类型明确跳过并提示「未实现」，给播报留位置，但不把未实现能力写成已完成。

### 10.2 默认参数与依据

| 设置 | 默认 | 依据 |
| --- | --- | --- |
| `refillThreshold` | 2 | 真实 Codex 一次约 13～16s、约 2.28 万 token；歌曲 3～5 分钟，留 2 首约 6～10 分钟缓冲，远超生成耗时，又不会过早空转 |
| `refillBatchSize` | 5 | 与既有 `pickCount` 一致；5 首约 15～25 分钟，约等于每 20 分钟一次 Codex 调用 |
| `refillBackoffBaseMs` / `refillBackoffMaxMs` | 30000 / 300000 | 指数退避，首次 30s，上限 5 分钟，避免紧密重试 |
| `refillMaxAttempts` | 5 | 同一轮最多自动重试 5 次，之后等待用户动作 |
| `codexFailureCooldownMs` / `MaxMs` | 60000 / 900000 | Codex 失败后先跳过订阅用曲库候选，冷却 1 分钟起、翻倍到 15 分钟上限 |

### 10.3 修改文件

```
server/orchestrator.js   新增：候选抽样、可播性校验、补歌协调（并发去重／意图作废／冷却／降级）
server/index.js          新增 POST /api/queue/refill；停止会话作废在途补歌；相关测试钩子
server/db.js             新增 7 个补歌设置默认值
server/netease.js        新增测试注入：音源接口整体报错
server/codex.js          新增 slow／success 注入模式、调用计数与延迟统计
public/orchestrator.js   新增：客户端补歌控制器（阈值、在途去重、epoch、退避、等待恢复）
public/app.js            追加式队列、补歌触发点、队列末尾等待、暂停/停止/换源边界、类型接口
public/index.html        引入 orchestrator.js，新增后台补歌状态区
public/style.css         自动补歌标记与状态样式
scripts/verify-orchestration.mjs  新增编排验证（自建独立服务 + 临时 SQLite）
scripts/verify-session.mjs        支持 RADIO_DB_FILE 隔离，避免污染真实数据
package.json             新增 verify:orchestration
```

### 10.4 验证结果

**第二轮 `npm run verify:orchestration`：40 通过 / 0 失败**。脚本自建独立服务进程与临时 SQLite（不写真实设置／反馈／播放记录），证据：`artifacts/orchestration.json`。其中自然续播断言仍可能在新曲解析期间提前通过，修正与补证见 10.7。

| 分组 | 项数 | 要点 |
| --- | --- | --- |
| 客户端控制器单元 | 13 | 阈值触发、在途去重、1s→2s 退避、上限后等待恢复、取消作废迟到结果、本地取消不发过期请求、追加 0 首按失败处理、新意图作废、会话失效后自动重试 |
| 服务端协调 | 11 | 同意图并发只生成一批（Codex 只调 1 次）、新意图作废旧任务、**乱序旧请求被拒且不重复调用**、排除当前/待播且不重复、逐首可播性校验、超时降级、冷却期不再调用、候选耗尽、no_playable、music_unavailable、停止作废 |
| 真实 Codex | 1 | 13.9s、22,833 token、exit 0，返回 3 首可播并附中文理由 |
| 真实浏览器 | 15 | 参数来自设置、后台只追加不打断、**第一首自然播完（198.6s）自动接入补歌批次**、队列播完进入等待后续上、**已播曲目重新补入并越过原队尾继续播**、慢响应不阻塞、暂停迟到结果不出声、停止后不污染、刷新不重复、快速切歌不被覆盖、降级续播且**降级批次真实播放**、候选不足进入等待恢复 |

**回归（独立数据库）**：`read-library` 退出码 0、`failures: []`；`verify:playback` 14/14；`verify:fixes` 18/18；`verify:session` 25/25；`verify:codex --skip-real` 9/9（真实 Codex 路径由本次编排验证覆盖）。

**跨批续播的观察方式（收窄后的准确表述）**：真实浏览器里用曲库中最短的一首（198.6s）作为第一首，让它**完整自然播完、触发真实的 `ended`**，然后自动接上后台补充的一批并继续出声——这条主链路没有拖进度。其余边界（队列播完后的等待、已播曲目重新补入后越过原队尾、降级批次续播）为了控制时长，用 `seekToEnd()` 把当前曲目拖到接近结尾来触发真实的 `ended` 事件：事件与解码都是真实的，但不是完整曲目自然播完。完整的「自然播完＋自动续播下一首」由 `verify:playback`（260s 曲目、ended 于 259.99s）另行覆盖。第一轮报告曾写「让它自然切到第二首」，实际当时两处边界都用拖动触发，该表述已在此更正。

**副作用隔离**：新增验证使用临时数据库与独立端口；`verify-session.mjs` 增加 `RADIO_DB_FILE` 支持，回归也用独立数据库，不污染真实反馈／设置／播放记录。历史真实 Codex 证据 `codex-plan.json` 未覆盖。

### 10.5 未完成／未验证

- **未做两小时连续收听**；跨批主链路已是自然播完，但其余边界仍用拖动加速，不能宣称完成包含 DJ、开场和聊天的完整产品验收。
- 真实订阅额度耗尽／真实限流未从服务侧触发，用可控注入验证。
- 试听片段（trial）分支仍未用真实样本验证（账号是 VIP）。
- DJ/TTS、天气、飞书日历、聊天、每日调度与 70%／30% 探索未实现。
- 多标签页／多客户端并发补歌未验证；单会话内已做过重复生成与乱序防护。
- 网页状态文案为中文简版，未做视觉走查与无障碍检查。

### 10.6 复核修复：两个真实缺陷（2026-09-16 第二轮）

复核 `da774d3` 时用当前源码独立复现出两个问题，均已修复并补上回归：

**缺陷 1：默认曲库播到底仍会停止。** 网页端 `appendPicks()` 按整个历史队列去重，而发给服务端的排除范围只有「当前＋待播＋已失败」。播到 456 首曲库末尾时，服务端返回的候选大多已在历史队列里，被全部过滤成「补充 0 首」，随后停止播放——正好打在「不再因队列耗尽而停止」这个目标上。
修复：客户端过滤范围与发送给服务端的排除范围统一为「当前＋待播＋已失败」，已播过的历史曲目允许重新入队；同时把「实际追加 0 首」作为失败处理（进入退避并明确提示），不再假装补充成功、也不会借此跳到队尾停止。当前曲高亮改为按位置标记，避免同一首歌重复入队后串高亮。
回归：真实浏览器里强制服务端返回一首已播过的歌，断言它被追加、越过原队尾后继续播放该曲（`完整队列播到底后继续播放`）；控制器单元断言追加 0 首时进入退避。

**缺陷 2：迟到的旧请求会取消新请求。** 服务端只判断「epoch 不同就作废在途任务」，没比大小。乱序场景下，先到的第 2 代被迟到的第 1 代取消，还重复调用了一次 Codex。
第二轮修复：服务端在生成期间拒绝更旧的请求（`superseded`），不影响在途的新任务；当时任务完成即清除水位，仍会接受完成后迟到的旧请求，后续修正见 10.7。客户端 epoch 改为基于页面启动时间，并在发送前检查本地是否已被取消，不再把过期请求发出去。
回归：先发起 epoch 200（慢响应），300ms 后再发 epoch 100，断言旧请求被拒、新请求正常完成、Codex 只调用 1 次；控制器单元断言本地取消后请求根本不会发出。

修复后全套重新验证：`verify:orchestration` 40/40；回归 `read-library` 退出码 0、`verify:playback` 14/14、`verify:fixes` 18/18、`verify:session` 25/25、`verify:codex --skip-real` 9/9（真实 Codex 路径由本轮编排验证覆盖）。

### 10.7 TDD 收尾：会话意图保留与自然续播证据（2026-09-16）

基于 `e9862e0`，限定修复两处复核缺口。测试边界为现有 `POST /api/queue/refill` 的请求结果，以及浏览器音频元素的播放观测。

**第一条 RED → GREEN：旧请求不能因新任务完成而重新有效。** 先添加“epoch 200 完成后再请求 epoch 100”的 HTTP 回归：修复前旧请求被接受，注入的 Codex 调用数为 2，结果为 [24 通过 / 1 失败](artifacts/tdd-closeout/server-red/orchestration.json)。然后将最新意图代次独立保留到会话结束；完成时只清除在途任务。修复后旧请求返回 `superseded`、调用数保持 1，且同一代次仍可生成下一批；包含控制器与服务端的 [26 项检查全部通过](artifacts/tdd-closeout/server-green/orchestration.json)。

**第二条 RED → GREEN：歌名变化不等于音频开始播放。** 从原浏览器判定中提取可独立验证的观测规则。首先用“currentId 已变、loadedId 仍旧、旧音频 ended=true”的快照使测试失败；再用“新音源匹配但播放进度没增长”使测试失败。修正后要求前后两个快照都属于同一首补入曲、实际音源一致、未暂停／结束／加载中、音频就绪且进度增长。`npm test` 的 3 项检查通过。

自然跨批验证另行记录真实音频的 `playing`／`ended`、单调时钟耗时、拖动与倍速事件；只有原曲从起点以正常速度完整结束，且新曲实际加载并推进，才算通过。原先只检查 `currentId` 和 `paused` 的证据不足已修正。

**最终结果：3 项离线测试通过；[编排回归 41 通过 / 0 失败](artifacts/tdd-closeout/browser-green/orchestration.json)**（控制器 13、服务端 13、真实浏览器 15；真实 Codex 调用本轮跳过）。自然曲实际结束位置与音频时长均为 **198.613333 秒**，正常速度，0 次拖动，观测墙钟耗时 **200.6523 秒**。补入曲 `currentId = loadedId = 2066755290`，播放进度从 **0.043648 秒增长到 1.045175 秒**，状态为“播放中”；详见产物 `evidence.naturalContinuation` 和 [续播截图](artifacts/tdd-closeout/browser-green/shot-10-crossbatch-playing.png)。

本轮还覆盖了暂停／停止／刷新／快速切歌、已播曲重新入队、降级曲实际播放，以及候选耗尽后的有界重试。其余边界仍采用注入或拖近结尾加速；本轮不代表两小时连续收听验收，也不代表新增真实 Codex 调用验证。

复现命令：

```sh
npm test
RADIO_REPORT_OUT=.scratch/radio-agent/verification/artifacts/tdd-closeout/server-green node scripts/verify-orchestration.mjs --server-only --skip-real
RADIO_REPORT_OUT=.scratch/radio-agent/verification/artifacts/tdd-closeout/browser-green node scripts/verify-orchestration.mjs --skip-real
```

测试服务与数据库独立；Codex 使用现有故障注入，本轮跳过真实模型调用。音乐与浏览器播放使用真实链路。临时服务目录完成后移入废纸篓，保留上述验收证据。
